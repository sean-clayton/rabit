<!DOCTYPE html>
<html>
<head>
  {{> head}}
</head>
<body unresolved class="{{body_class}}">
<a href="{{{block "fab-action"}}}"><paper-fab class="rabit fab" icon="{{{block "fab-icon"}}}"></paper-fab></a>
  <div id="blog">
{{{body}}}
  </div>
  {{#is "index"}}
    {{pagination}}
  {{/is}}
  {{> footer}}
</body>
</html>
